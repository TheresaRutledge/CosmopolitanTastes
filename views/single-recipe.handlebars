<article class="recipe">
  <div class="title">
    <a href="{{recipeData.recipe_url}}" target="_blank">{{recipeData.title}}</a>
    <img src="../photos/{{recipeData.picture}}" alt="{{recipeData.title}}">
    <p>{{recipeData.ingredients}}</p>
    <p>{{recipeData.instructions}}</p>
  </div>
  {{!-- <div class="meta">
    {{recipeData.vote_count}} point(s) by {{recipeData.user.username}} on
    {{recipeData.created_at}}
    |
    <a href="/recipe/{{recipeData.user_id}}">{{recipeData.comments.length}} comment(s)</a>
  </div> --}}
</article>

{{!-- <form class="comment-form">
  <div>
    <textarea name="comment-body"></textarea>
  </div>

  <div>
    <button type="submit">add comment</button>
    <button type="button" class="upvote-btn">upvote</button>
  </div>
</form>

<div class="comments">
  {{#each recipeData.comments}}
  <section class="comment">
    <div class="meta">
      {{user.username}} on {{created_at}}
    </div>
    <div class="text">
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</div> --}}

{{#if loggedIn}}
<script src="/javascript/comment.js"></script>
<script src="/javascript/upvote.js"></script>
{{/if}}